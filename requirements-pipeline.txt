# Pipeline-only dependencies (not needed by the serving API)
# Install with: pip install -r requirements-pipeline.txt

pandas>=2.0.0
pyyaml>=6.0
mlflow>=2.10.0
dvc>=3.0.0
dvc[s3]>=3.0.0
xgboost>=2.0.0
evidently>=0.4.0
matplotlib>=3.7.0
seaborn>=0.12.0
# These are already in requirements.txt but listed here for standalone pipeline runs
scikit-learn>=1.2.0
numpy>=1.24.0
